const {
  ZERO_ADDRESS,
  ZERO_BYTES32,
  EVMError,
  vidcoin,
  randInt,
  ensuresException,
  addDays, 
  addSeconds,
  mineBlock,
  mineNBlocks,
} = require('./utils');

const {
  register
} = require('./registrar');

module.exports = {
  ZERO_ADDRESS,
  ZERO_BYTES32,
  EVMError,
  vidcoin,
  randInt,
  ensuresException,
  addDays, 
  addSeconds,
  mineBlock,
  mineNBlocks,
  register
};
