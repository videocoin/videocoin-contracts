const {
  ZERO_ADDRESS,
  ZERO_BYTES32,
  EVMError,
  vidcoin,
  randInt,
  ensuresException,
  addDays, 
  addSeconds,
  mineBlock,
  mineNBlocks,
} = require('./utils');

module.exports = {
  ZERO_ADDRESS,
  ZERO_BYTES32,
  EVMError,
  vidcoin,
  randInt,
  ensuresException,
  addDays, 
  addSeconds,
  mineBlock,
  mineNBlocks
};
